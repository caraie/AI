knn
===

k nearest neighbours with euclidean distance and majority vote, plus a
leave one out check over a few rows of the iris data set.

    python knn.py       accuracy for k = 1, 3, 5
    python test_knn.py  tests
